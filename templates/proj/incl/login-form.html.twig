{{ include('flash.html.twig') }}

<div class="wrapper login">
<form method="post" action="{{ path('login') }}">
<h2>Login to play</h2>
    <div class="input-box">
    <span class="material-symbols-outlined icon">
    alternate_email
    </span>
    <input name="email" type="email" required="required" minlength="1" maxlength="100" placeholder="Email">
    </div>

    <div class="input-box">
    <span class="material-symbols-outlined icon">key</span>
    <input name="password" type="password" required="required" minlength="1" maxlength="100" placeholder="Password">
    </div>
    <button class="form-btn" type="submit">Login</button>
    <div class="register-link">
    <p>Don't have an account? <a href="{{ path('register-form') }}">Register</a></p>
    </div>
</form>
</div>
