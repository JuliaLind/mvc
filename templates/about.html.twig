{% extends "base.html.twig" %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    {# 
    <p>An image which can be clicked on.</p>
    <a href="{{ asset('img/glider.svg') }}">
        <img src="{{ asset('img/glider.svg') }}" alt="">
    </a> #}
    <div class="grid-container two-col golden-ratio">
        <div class="col-2">
            {{ text | raw }}
        </div>
        <div class="col-1 mvc-image" style="background-image: url({{ asset('img/about.jpg') }})">
        </div>
    </div>
{% endblock %}